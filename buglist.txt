EXPORT
    > tem que funcionar sem argumentos (segfault)
    > esta a adicionar na lista errada. 

PIPES 
    > (fixed) Criar fork para builtins
    > (fixed) cria forks para comandos que nao existem
    > (fixed) bad FD com mais do que um PIPE
    > (fixed) Nenhum comando funciona se usar o full path para ele. (ex: /bin/ls)

EXIT 
    > (in progress) Nao faz free a nada

ENV 
    > (ta feito) imprime um NULL no fim 

EXPAND DOLETA
    > (fixed)"echo $PWD/a" esta a dar segfault

CD 
    > (fixed) Se fizer unset ao oldpwd, dá segfault. o mesmo deve acontecer se fizer unset ao PWD, nao testei
    > (fixed - adolfo)cd .. fica com .. no caminho
    > (fixed - adolfo)cd / nao fez um caralho
    > (fixed - adolfo)cd ../../bde-sous/Documents/42cursus/../  --> PWD=/nfs/homes/bde-sous/Documents/../../bde-sous/Documents/42cursus/../
    
LEXER / Parsing
    > input "pwd |wc -l" separado em  
        pwd = comando 
        |wc = flag  
        -l = flag

Redirects
    > (fixed) Redirect de builtins
    > (fixed) Estou a dar Open dos FD's mas nunca lhes dou close.

MISSING FEATURES
    > Heredoc
        Multiple Heredoc processa do primeiro ate ao ultimo e usa o ultimo para o comando seguinte
    > Exit Codes
    > Memory Leaks
    > Normas
